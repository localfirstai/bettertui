# @bettertui/devtools

**Developer tools and debugging (intended).** No internal dependencies. **Stub.**

## Exports

| Export | Type | Notes |
|--------|------|-------|
| `DevToolsOptions` | `interface` | `{ enabled: boolean; port: number }` |
| `createDevTools(_options?: Partial<DevToolsOptions>): unknown` | function | **always returns `null`** |

## Status

No functionality. The inspector/profiler/error-overlay envisioned in the architecture is not implemented. There is no `@bettertui/devtools` runtime yet.
