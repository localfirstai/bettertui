# Widgets

The native widget host is exposed through `@bettertui/core`'s native bridge (`createWidgetHost`).
Internally it lives in the `bettertui-engine` crate (there is no separate `bettertui-widgets`
crate). See [Architecture: Widget Model](../../architecture/widget-model.md) and
[Widgets doc](../../widgets.md).
