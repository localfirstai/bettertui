pub struct CapabilityDetector;

impl Default for CapabilityDetector {
    fn default() -> Self {
        Self::new()
    }
}

impl CapabilityDetector {
    pub fn new() -> Self {
        Self
    }
}
