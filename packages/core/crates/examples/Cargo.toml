[package]
name = "bettertui-examples"
version = "0.0.0"
edition = "2024"
license = "MIT"
description = "Native Rust examples for BetterTUI engine"

[dependencies]
bettertui-engine = { path = "../engine" }
bettertui-widgets = { path = "../widgets" }
bettertui-terminal = { path = "../terminal" }
bettertui-logger = { path = "../logger" }
crossterm = "0.28"
tracing = "0.1"
tracing-subscriber = "0.3"
