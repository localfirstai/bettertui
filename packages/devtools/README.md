# @bettertui/devtools

Developer tooling for BetterTUI.

## What's inside

- `createDevTools()` — the public entry point. Currently a **stub**: it returns `null`.

A future release will add an inspector, profiler, and error overlay. The package is wired into the workspace (build, lint, typecheck, test) but exposes no functional API yet.

## Status

Stub. `createDevTools()` returns `null`.

See [`docs/api/packages/devtools.md`](../../docs/api/packages/devtools.md).
