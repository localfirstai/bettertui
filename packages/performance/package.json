{
  "name": "@bettertui/performance",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "description": "Independent benchmark harness comparing published OpenTUI vs BetterTUI. Consumes npm packages only (no workspace references).",
  "license": "MIT",
  "scripts": {
    "bench": "bun run src/bench/runner.ts",
    "dev": "bun run src/visual/side-by-side.tsx",
    "build": "astro build",
    "deploy": "astro build && echo 'Deploy dist/ to performance.bettertui.com'"
  },
  "dependencies": {
    "@opentui/core": "0.4.3",
    "@opentui/react": "0.4.3"
  },
  "devDependencies": {
    "astro": "^6.4.6",
    "react": "^19.0.0",
    "typescript": "^5"
  },
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=20"
  }
}
