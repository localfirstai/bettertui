{
  "name": "@bettertui/examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsdown --watch",
    "build": "tsdown",
    "build:standalone": "node scripts/build.ts",
    "start": "node dist/index.mjs",
    "example": "node scripts/run-example.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@bettertui/core": "workspace:*",
    "@bettertui/react": "workspace:*",
    "@bettertui/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
