{
  "name": "example-NAME",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsdown src/index.tsx --format esm --watch",
    "build": "tsdown src/index.tsx --format esm",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@bettertui/core": "workspace:*",
    "@bettertui/react": "workspace:*",
    "@bettertui/shared": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
